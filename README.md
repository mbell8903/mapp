# sales-mapp

Install Instructions
- Install node.js
- Navigate into the new directory
- Run `npm install bower grunt grunt-cli -g`
- Run `npm install`
- Run `bower update`
- Run `grunt build`

Running the app
- Run `node app/app.js`
